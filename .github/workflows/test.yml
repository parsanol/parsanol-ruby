---
name: Test gems

on:
  workflow_dispatch:
  push:
    branches: [main, master]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci-data:
    runs-on: ubuntu-22.04
    outputs:
      result: ${{ steps.fetch.outputs.result }}
    steps:
      - id: fetch
        uses: oxidize-rb/actions/fetch-ci-data@v1
        with:
          supported-ruby-platforms: |
            exclude: [arm-linux, x64-mingw32]
          stable-ruby-versions: |
            exclude: [head]

  build:
    name: Build gems
    uses: ./.github/workflows/gem-build.yml

  test:
    name: Test ${{ matrix.ruby-platform }} (Ruby ${{ matrix.ruby }}) on ${{ matrix.os }}
    needs: [ci-data, build]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux x86_64
          - os: ubuntu-22.04
            ruby-platform: x86_64-linux
            ruby: "3.2"
          - os: ubuntu-22.04
            ruby-platform: x86_64-linux
            ruby: "3.3"
          - os: ubuntu-22.04
            ruby-platform: x86_64-linux
            ruby: "3.4"
          - os: ubuntu-22.04
            ruby-platform: x86_64-linux
            ruby: "4.0"

          # Linux ARM64
          - os: ubuntu-22.04-arm
            ruby-platform: aarch64-linux
            ruby: "3.2"
          - os: ubuntu-22.04-arm
            ruby-platform: aarch64-linux
            ruby: "3.3"
          - os: ubuntu-22.04-arm
            ruby-platform: aarch64-linux
            ruby: "3.4"
          - os: ubuntu-22.04-arm
            ruby-platform: aarch64-linux
            ruby: "4.0"

          # macOS Intel
          - os: macos-15-intel
            ruby-platform: x86_64-darwin
            ruby: "3.2"
          - os: macos-15-intel
            ruby-platform: x86_64-darwin
            ruby: "3.3"
          - os: macos-15-intel
            ruby-platform: x86_64-darwin
            ruby: "3.4"
          - os: macos-15-intel
            ruby-platform: x86_64-darwin
            ruby: "4.0"

          # macOS Apple Silicon
          - os: macos-15
            ruby-platform: arm64-darwin
            ruby: "3.2"
          - os: macos-15
            ruby-platform: arm64-darwin
            ruby: "3.3"
          - os: macos-15
            ruby-platform: arm64-darwin
            ruby: "3.4"
          - os: macos-15
            ruby-platform: arm64-darwin
            ruby: "4.0"

          # Windows x64
          - os: windows-2025
            ruby-platform: x64-mingw-ucrt
            ruby: "3.2"
          - os: windows-2025
            ruby-platform: x64-mingw-ucrt
            ruby: "3.3"
          - os: windows-2025
            ruby-platform: x64-mingw-ucrt
            ruby: "3.4"
          - os: windows-2025
            ruby-platform: x64-mingw-ucrt
            ruby: "4.0"

          # Windows ARM64 (Ruby 3.4+ only)
          - os: windows-11-arm
            ruby-platform: arm64-mingw-ucrt
            ruby: "3.4"
          - os: windows-11-arm
            ruby-platform: arm64-mingw-ucrt
            ruby: "4.0"
    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            .github/scripts

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}

      - uses: actions/download-artifact@v4
        with:
          name: cross-gem-${{ matrix.ruby-platform }}
          path: pkg/

      - name: Install gem
        run: gem install pkg/parsanol-*.gem --verbose

      - name: Test installed gem
        run: ruby .github/scripts/test_installed_gem.rb
